# Data directory

The application expects the CMS manual at:

`data/2026-ncci-medicare-policy-manual-all-chapters.pdf`

The generated SQLite index is written to `data/ncci.db` and is intentionally excluded from version control. Rebuild it with `python app.py ingest`.

